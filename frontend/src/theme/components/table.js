// frontend/src/theme/components/table.js

const Table = {
  baseStyle: {}, // No base styles, reset to defaults
  sizes: {}, // No custom sizes
  variants: {}, // No variants
  defaultProps: {}, // No forced size or variant
};

export default Table;
