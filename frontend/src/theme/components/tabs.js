// frontend/src/theme/components/tabs.js

const Tabs = {
  baseStyle: {}, // No custom base styles
  sizes: {}, // Use Chakra defaults
  variants: {}, // No custom variants
  defaultProps: {}, // No forced variant
};

export default Tabs;
