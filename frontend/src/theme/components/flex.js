// frontend/src/theme/components/flex.js

const Flex = {
  baseStyle: {}, // no custom base styles
  variants: {}, // remove all custom variants
  sizes: {}, // optional, can keep empty
  defaultProps: {}, // no default props
};

export default Flex;
