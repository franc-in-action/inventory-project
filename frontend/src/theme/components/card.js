// frontend/src/theme/components/card.js

const Card = {
  baseStyle: {}, // No default styles
  sizes: {}, // No custom sizes
  variants: {}, // No variants
  defaultProps: {}, // No forced size or variant
};

export default Card;
